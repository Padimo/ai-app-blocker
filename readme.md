# Welcome to the AI App Blocker! 

This application uses AI image recognition (powered by Claude) to detect when the user has completed a task that they've committed to doing. It uses Swift (is a Mac application) and currently only closes applications (doesn't block websites yet). 

### Features list: 
- Block all except list
- Block websites
- Unblock with Claude